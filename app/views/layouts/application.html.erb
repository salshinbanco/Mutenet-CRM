<!DOCTYPE html>
<html>
<head>
  <title>MutenetCrm</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<% if notice %>
  <p class="alert alert-notice"><%= notice %></p>
<% end %>
<% if alert %>
  <p class="alert alert-error"><%= alert %></p>
<% end %>

<div id='user_nav'>
  <% if user_signed_in? %>
    現在、<%= current_user.email %>でログインしています. <%= link_to "ログアウト", destroy_user_session_path, :method => :delete %>
  <% else %>
    <%= link_to "新しいユーザーを作成", new_user_registration_path %> する
    <br>
    <%= link_to "ログイン画面へ", new_user_session_path %>
  <% end %>
</div>

<%= yield %>

</body>
</html>
